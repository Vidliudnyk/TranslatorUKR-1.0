# TranslatorUKR-1.0
**Автоматичний ШІ перекладач та редактор файлів. Діалоги, UI, субтитри та інше.**

**TranslatorUKR** — це інструмент для автоматизованого перекладу файлів локалізації ігор та програм на українську мову за допомогою штучного інтелекту (LLM). Програма створена для того, щоб полегшити рутинну роботу перекладачів, зберігаючи структуру файлів та технічні теги.

![Main App Interface](https://github.com/user-attachments/assets/10d21fe8-fb34-43a7-a1fc-88a11a160ac5)

> [!WARNING]
> **Важливе попередження!**
> Автор програми наголошує: **ШІ-переклад не є ідеальним!**
> Використовуйте цю програму для створення чорнового варіанту (фундаменту), який обов'язково потребує подальшого ручного редагування та вичитки.

---

## ✨ Основні можливості

* **Підтримка багатьох форматів:** `.txt`, `.json`, `.xml`, `.ini`, `.po`, `.lua`, `.yaml`, `.srt` та інші.
* **Розумний переклад:** Зберігає код, змінні, теги кольорів та форматування недоторканими.
* **Гнучкість:** Працює з:
    * **Хмарними API:** OpenAI, Anthropic, DeepSeek, Google.
    * **Локальними моделями:** Ollama, LM Studio.
* **Зручний інтерфейс:** Темна тема, підсвічування синтаксису, вбудований глосарій та статистика.
* **Розваги:** Вбудовані міні-ігри (Змійка, Понг, Flappy Bird), щоб не нудьгувати під час очікування перекладу.

---

## 🚀 Як користуватися

### 📝 Перед початком: Робота з ігровими архівами
Часто файли локалізації запаковані всередині ігрових ресурсів. Перед перекладом їх необхідно розпакувати спеціальними утилітами.

| Рушій / Гра | Рекомендований інструмент |
| :--- | :--- |
| **Unreal Engine** | `FModel` (перегляд та експорт) |
| **Unity** | `UABEA` або `UnityEX` |
| **REDengine** | `WolvenKit` |
| **Bethesda** | `B.A.E.` — Bethesda Archive Extractor |
| **Source Engine** | `GCFScape` або `VPKEdit` |
| **FromSoftware** | `UXM` |
| **Godot** | `Godot PCK Explorer` |
| **GameMaker Studio** | `UndertaleModTool` |
| **RPG Maker** | `RPG Maker Decrypter` або `EnigmaVBUnpacker` |

---

### КРОК 1: Налаштування LLM

#### Варіант А: Для хмарних API (наприклад, DeepSeek)
1. Виберіть провайдера зі списку (наприклад, `DeepSeek`).
2. Введіть ваш API ключ у відповідне поле.
3. Виберіть модель зі списку або введіть власну назву моделі.

#### Варіант Б: Для локальних LLM (LM Studio)
1. Завантажте та встановіть **[LM Studio](https://lmstudio.ai)**.
2. Завантажте модель, наприклад:
3. Запустіть локальний сервер у LM Studio (вкладка **Local Server**).
4. У програмі виберіть провайдера **LM Studio (Local)**.
5. Налаштування:
* **Base URL:** залиште `http://localhost:1234/v1`
* **API ключ:** залиште порожнім або введіть `lm-studio`
* **Своя модель:** введіть назву завантаженої моделі (наприклад, `mamaylm-gemma-3-12b-it-v1.0@q8_0`).

### КРОК 2: Вибір файлу
1. Натисніть кнопку **"Вибрати файл"**.
2. Виберіть файл (підтримуються всі текстові формати).
3. Оригінальний текст з'явиться в лівій панелі.

### КРОК 3: Налаштування глосарію (Опціонально)
1. Натисніть **"Глосарій"** для відкриття редактора термінів.
2. Додайте терміни, які мають перекладатися однаково.
* *Приклад:* `Health` → `Здоров'я`, `Mana` → `Мана`.

### КРОК 4: Переклад
1. Натисніть **"Почати переклад"**.
2. Спостерігайте за прогресом у реальному часі (переклад з'являється справа).
3. *Порада:* Під час очікування натисніть кнопку **"🎮 Ігри"**.

### КРОК 5: Редагування та збереження
1. Перегляньте отриманий переклад.
2. Натисніть **"Редагувати"** для внесення правок вручну (Ctrl+F для пошуку).
3. Натисніть **"Зберегти переклад"**.
* Файл збережеться з суфіксом `-ukr` (наприклад: `game-ukr.json`).

---

## 💰 Підтримка автора

Якщо програма стала вам у пригоді, ви можете подякувати автору:

💳 **`4441 1111 3424 9402`**
